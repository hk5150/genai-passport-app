# 生成AIパスポート対策アプリ — Claude Code引き継ぎメモ

## 0. デプロイ先
- 公開URL: https://hk5150.github.io/genai-passport-app/
- リポジトリ: https://github.com/hk5150/genai-passport-app (public, GitHub Pages配信)
- デプロイ方法: `main` ブランチのルートを GitHub Pages がそのまま配信(ビルドなし)。
  更新は `git push origin main` するだけで数十秒〜数分で反映される。
- **バージョン表示(2026-08-12追加)**: ホーム画面のタイトル横に `app.js` 冒頭の
  `APP_VERSION` 定数(例: `v1.3.0`)がバッジ表示される。**リリース(git push)ごとに
  このバージョンを更新すること**(セマンティックバージョニング目安: 問題追加や
  軽微な修正は patch、新機能追加は minor、大規模な作り直しは major)。
  静的ファイル(`app.js`/`index.html`/`styles.css`/`manifest.json`)を1文字でも
  変更した場合は、`sw.js` の `CACHE_NAME` も必ず版数を上げること
  (cache-first戦略のため、上げないとユーザーの端末に古いapp.jsが残り続け、
  バージョン表示自体も更新されない)。

## 1. これは何か
生成AIパスポート試験(GUGA主催)の対策用クイズアプリ。公式テキスト全5章の
目次構成に沿って、オリジナルの4択問題+解説を307問収録している。
模擬試験モード(ランダム60問)、分野別演習、間違えた問題の復習キューを実装済み。

現状はビルド不要の素のHTML/CSS/JSで動作する状態。Claude Codeでの今後の作業は
主に「問題を増やす」「Vite化する」「PWA化してデプロイする」の3方向を想定。

## 2. ファイル構成
```
genai-passport-app/
├── index.html      画面のマークアップ(3画面: ホーム/クイズ/結果) + PWAメタタグ
├── styles.css       全スタイル(CSS変数でテーマカラーを管理)
├── app.js           アプリロジック(状態管理・出題・採点・localStorage・SW登録)
├── questions.json   問題データ本体(307問)
├── manifest.json    PWAマニフェスト(アプリ名・アイコン・テーマカラー)
├── sw.js            Service Worker(オフラインキャッシュ)
├── icons/           PWAアイコン(icon-192/512, maskable-512, apple-touch-icon)
└── .claude/launch.json  プレビュー用サーバー設定(npx serve . -l 5173)
```
ビルドステップなし。`index.html` を静的サーバーで配信すればそのまま動く。
`file://` で直接開くと `fetch('./questions.json')` がCORSでブロックされる
ブラウザがあるので、開発時は簡易サーバー経由で見ること
(例: `npx serve .` や `python3 -m http.server`)。

### PWA対応(2026-08-12追加)
`manifest.json` + `sw.js` によりホーム画面に追加してアプリのように使える。
- Service Workerはアプリシェル(html/css/js/manifest/icons)をキャッシュ優先で配信、
  `questions.json` はネットワーク優先(オフライン時のみキャッシュにフォールバック)。
- キャッシュ名は `genai-passport-v2`(2026-08-12に問題拡充+app.js変更を機にv1→v2へ更新)。
  **index.html/app.js/styles.css/manifest.jsonを変更したら必ずこの版数を上げること。**
  Service Workerはcache-first戦略のため、版数を上げないと開発中のブラウザでも
  古いapp.jsがキャッシュから返り続け、修正が反映されない(実際にqKeyの修正が
  一度反映されず要デバッグとなった)。questions.jsonはnetwork-firstなので
  この問題は起きないが、app.js等の静的アセットは要注意。
- アイコンは `scripts/gen_icons.py` 相当のPillowスクリプトで生成した簡易デザイン
  (紺地にAIロゴ)。本格運用時はデザインを差し替え推奨。
- iOS Safari は `apple-mobile-web-app-*` メタタグと `apple-touch-icon` で
  ホーム画面追加時にスタンドアロン表示になる。Android Chromeは`manifest.json`から
  自動でインストールバナーが出る。
- ネイティブアプリ化(Capacitor等でのApp Store/Google Play配信)は未実施。
  必要になったら要相談。

## 3. データモデル (questions.json)
各問題は以下のフラットな構造:
```json
{
  "id": "ch1-001",         // 章内で連番のユニークID(2026-08-12追加、qKeyのハッシュに使用)
  "ch": 1,                 // 章番号 1〜5 (下記マッピング参照)
  "sec": "AIの定義",        // 章内のセクション名(自由文字列)
  "q": "問題文",
  "c": ["選択肢1","選択肢2","選択肢3","選択肢4"],
  "a": 0,                  // 正解のインデックス(0始まり、c配列に対応)
  "e": "解説文"
}
```
`id` は `ch{章番号}-{章内連番3桁}` 形式(例: `ch3-045`)。既存問題からの追記順に
振っているだけなので、章内の並び順に特に意味はない。新しい問題を追加する場合は
その章の最大連番+1から採番すること。
章番号マッピング(公式テキストの目次に対応):
| ch | 内容 |
|----|------|
| 1 | AI(人工知能) — 定義, 機械学習, AIの種類, 歴史, シンギュラリティ |
| 2 | 生成AI(ジェネレーティブAI) — 誕生の歴史(CNN/VAE/GAN/RNN/Transformer), ChatGPT, Gemini/Claude/Copilot |
| 3 | 現在の生成AIの動向 — マルチモーダル生成AI, ディープフェイク, RAG, AIエージェント/MCP |
| 4 | 情報リテラシー・基本理念とAI社会原則 — セキュリティ, 個人情報保護法, 知的財産権, AI社会原則, AI事業者ガイドライン, AI新法 |
| 5 | テキスト生成AIのプロンプト制作と実例 — LM/LLM, プロンプティング, ビジネス応用, 不得意なこと |

章名の日本語表示は `app.js` 冒頭の `CHAPTERS` オブジェクトで管理している。
章を増やす/名前を変える場合はここも合わせて更新すること。

現在の分野別内訳: 第1章63問 / 第2章68問 / 第3章60問 / 第4章63問 / 第5章53問(計307問)。

## 4. アプリの状態管理(app.js の要点)
- グローバル変数 `QUESTIONS`(問題配列)と `session`(受験中の状態)、
  `store`(localStorage永続データ: 受験履歴+誤答カウント)の3つが中心。
- `store` は `localStorage` キー `genai_passport_store_v1` に保存。
  - `store.history`: 受験ごとの `{date, mode, total, correct, pct, chapterOrNull}`
  - `store.wrong`: 問題キー→誤答回数のマップ。正解すると1減算、0で復習キューから外れる。
  - 問題キーは `qKey(q) = q.id || (q.ch + "|" + q.q.slice(0,12))`(2026-08-12更新)。
    `id` フィールドを追加したことで問題文の軽微な修正では復習履歴がリセットされなく
    なった。`id` を持たない旧データが万一残っていた場合のフォールバックとして
    旧ハッシュ式も残しているが、現行の questions.json は全問 `id` を持つ。
- 出題順・選択肢順は毎回シャッフル(`shuffle()`)。
- 3画面の切り替えは `screen(id)` で `.screen.active` クラスをトグルするだけの単純なSPA。

## 5. 実装済み機能
- ホーム画面: 受験回数・平均正答率・要復習数のサマリー表示
- 模擬試験モード: 全問題からランダムに最大60問出題
- 分野別演習: 章を選んでその章の全問題をランダム順で出題
- 復習モード: 誤答が残っている問題のみ出題(正解すると復習対象から除外)
- 出題画面: 章タグ/セクションタグ、進捗バー、選択→即時正誤判定→解説表示
  - 4択の下に「わからない」ボタン(2026-08-12追加)。押すと不正解として扱われ
    復習キューに入るが、どの選択肢も「誤答」としてはハイライトしない(正解のみ
    緑でハイライト)。フィードバックヘッダーはオレンジ「わからない」表示で、
    通常の不正解と同様に解説(`q.e`)を必ず表示する。実装は `app.js` の
    `answerQuestion(idx, chosenIdx)` で `chosenIdx=null` を渡すことで表現している。
- 結果画面: 総合スコア、章別正答率バー、誤答した問題の一覧+解説
  - 「わからない」で回答した問題は `wrongItem.unknown` / `wrongYour.unknown` クラスで
    オレンジ表示になり、「あなたの回答: わからない(未回答)」と表示される
    (通常の誤答は赤、こちらはオレンジで視覚的に区別)。
- localStorageによる永続化(端末内のみ、サーバー同期なし)

## 6. 既知の制限・注意点
- **著作権**: 公式テキスト(GUGA発行『生成AIパスポート公式テキスト』2026年試験
  シラバス対応版、全213ページ)の実物PDFを2026-08-12にユーザーから受け取り、
  内容確認・用語や数値の精度向上のために参照した。ただし本文のOCR結果や文章を
  そのままquestions.jsonに転記することは一切行っていない。各ページを読んで
  概念・数値・固有名詞を理解した上で、問題文・選択肢・解説はすべて独自の表現で
  書き起こしている。今後問題を追加する際も同じ方針を厳守すること
  (本文の逐語転記は著作権侵害のリスクがある)。
- **FujiCert等の他社問題集**: 内容を転用していない。分野バランスの参考にしたのみ。
- localStorageを使っているため、Claude.aiのartifactプレビュー内では正しく動作しない
  (artifactはlocalStorage非対応)。実運用は必ずデプロイ先(GitHub Pages等)で確認すること。
  OVERLOAD+と同様に、Claude.ai上でのプレビューはレイアウト確認程度に留める。
- 4択の中身自体はシャッフルされるが、正解の分布(0〜3のどこに正解が来やすいか)は
  未検証。長期的な公平性チェックは未実施。
- 307問中、既存186問と新規121問の間で完全一致する問題文は無いことをスクリプトで
  確認済みだが、表現違いの類似問題(言い換え)が紛れ込んでいる可能性はゼロではない。

## 7. 次のステップ候補(優先度は要相談)
1. **Vite移行**: OVERLOAD+と同じ流れ。`npm create vite@latest` でプロジェクト化し、
   `index.html`/`app.js`/`styles.css` をほぼそのまま移植可能(フレームワークなしの
   vanilla構成なので移行コストは低いはず)。
2. **問題数のさらなる拡充**: 現状307問(FujiCert 375問クラスに近づいた)。
   まだ深掘りできる余地がある分野: 第2章のGemini/Claudeの詳細な機能比較、
   第3章の音楽/音声生成AIサービス固有名詞、第4章のAI事業者ガイドライン各原則の
   詳細、第5章のプロンプトテクニック(CoT/ReAct等、公式テキストに記載があれば)。
   追加時は該当章のJSONに `id: "ch{n}-{章内最大連番+1}"` から採番して追記すればよい
   (アプリ側のコード変更は不要)。
3. ~~問題にユニークIDを付与~~ → 2026-08-12対応済み。全307問に `id` フィールドを
   付与し、`qKey()` をID優先に変更した。
4. **間隔反復(SRS)アルゴリズムの導入**: 現状は「誤答したら復習キューに入り、
   正解したら抜ける」という単純な仕組み。Ankiのような間隔反復(SM-2等)に
   発展させる余地あり。
5. ~~PWA化~~ → 2026-08-12対応済み(manifest.json + sw.js)。
6. **模擬試験の時間制限**: 公式試験相当(60問60分)を意識し、タイマー機能を追加する。
7. ~~公式テキストの精査~~ → 2026-08-12、ユーザーから提供された公式テキストPDF
   (全213ページ)を通読し、新規追加した121問はこの内容を踏まえて数値・固有名詞の
   精度を上げてある。既存186問との突き合わせ精査(誤り修正)はまだ未実施。

## 8. 動作確認方法
```bash
cd genai-passport-app
npx serve .          # または python3 -m http.server 8080
# ブラウザで http://localhost:xxxx/ を開く
```
